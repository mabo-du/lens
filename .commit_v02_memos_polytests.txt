feat(v0.2): memos-on-region binding + polygon-mode test coverage

Substantial FE-only followup to round-74/75 (v0.2 polygon backend +
frontend). Rust backend and migrations are unchanged.

1. Memos-on-region binding (image regions + polygons):
   - ImageViewer's right-click on a persisted region OR polygon now
     opens a small action Dialog with two buttons: Edit Memo... and
     Delete. Both flows dispatch through the existing memo backend.
   - Shape deletion flow moved entirely into the action Dialog (no
     direct delete on right-click).
   - Stage handler's e.target !== stage bubble guard from round-75
     is preserved.
   - Memo-presence badge (after code-name label) on shapes with a
     non-empty memo body.
   - No schema migration: memo.linked_selection_id already supports
     any selection type.

2. Polygon-mode test coverage (30 vitest tests):
   - Pure helpers extracted into src/components/editor/polygonState.ts
   - 30 vitest boundary-case tests added
   - ImageViewer imports the helpers; inline math removed.

3. Reviewer-blocker fixes (5 surgical edits):
   - RegionMemoDialog autosave try/catch/finally + toast.error
   - ImageViewer enforce MAX_POLYGON_VERTICES via early-return
   - handleModeChange delegates to modeSwitchReset helper
   - Removed unused effectiveCursor useMemo (TS6133 warning)
   - Imports cleaned up

Gates (after fixes): tsc 0 / vitest 0 (44/44) / vite build 0.
Cargo tests 57/57 (unchanged).
