v0.2-rc-poly - polygon region backend foundation

Persistence + IPC for free-form polygon annotations on image documents.
Schema migration 06 expands selection.selection_type CHECK to include
'image_polygon' and adds the image_polygon extension table (parallel of
image_selection for bbox regions). Three Tauri commands back the FE:
image_polygon_create with full 3..64-vertices validation in [0,1]^2,
list_by_document via JOIN, delete with selection_type guard. TypeScript
IPC types and invoke wrappers in src/ipc/image-polygons.ts. All gates
green (cargo 57/57, tsc clean, vitest 14/14, vite build clean).

Frontend UX (ImageViewer.tsx polygon mode) lands in the next round.
