v0.1.1 - Phase C-2: Konva image-viewer + plain_text NULL relaxation

Substantial followup to v0.1.0-rc.1's Phase C backend-only image imports.
Brings image documents to first-class UX parity with text/docx/pdf:
React/Konva canvas with drag-to-create bbox regions, code picker,
region list with right-click delete. Schema migration 05 relaxed the
document.plain_text NOT NULL constraint necessary to allow NULL on
image rows; FTS5 triggers re-engineered with COALESCE() to keep
search working. 5 new IPC handlers (3 image_region + 1 base64 asset
+ 1 dispatcher flip). All gates green at tag push (cargo test 56/56,
tsc clean, vitest 14/14, vite build clean).
